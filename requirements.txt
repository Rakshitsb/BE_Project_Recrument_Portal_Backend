bcrypt==4.0.1
fastapi
groq
httpx
motor
nanoid
passlib[bcrypt]
pdfplumber
pydantic[email]
pydantic-settings
python-dateutil
python-docx
python-dotenv
python-jose[cryptography]
python-multipart
retell-sdk
uvicorn
cloudinary

# ── RAG Chatbot & Vector Store (added in v2.1.0) ──────────────────────────
chromadb
sentence-transformers==2.7.0
# Render currently exposes newer CPU wheels, so avoid pinning an older Torch
# version that may not exist for the selected Python runtime.
torch>=2.9.0,<2.12.0
numpy>=2.0.0          # numpy 1.x has no Python 3.13 wheel; 2.x ships pre-built cp313 wheels
